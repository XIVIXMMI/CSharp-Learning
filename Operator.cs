using System;
namespace CSharp
{
	public class Operator
	{
		public Operator()
		{
		}
	}
}
/*Toán tử là gì
 * Là một công cụ được dùng để thao tác với dữ liệu 
 * Là một ký hiệu được dùng để đại diện cho một thao tác cụ thể thực hiện trên dữ liệu
 * có 6 loại toán tử
 * Toán tử toán học ( + , - , * , / , % , ++ , -- )
 * Toán tử quan hệ ( == , != , > , < , >= , <= )
 * Toán tử logic
 * Toán tử khởi tạo và gán ( = , += , -= , *= , /= ,%= )
 * Toán tử so sánh ( & , | , ^ , ~ , << , >> )
 * Toán tử khác ( sizeof(), typeof(), new, is, as, ?:, , )
 * 
 */
